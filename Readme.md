Example usage
============

Compile with:
```
gcc omb_hw.c
```
Run with:
```
./a.out 3 0 < input.txt
```
First argument is buffer size, second is cleanup boolean (for deleting buffer overflows saved to disk)
